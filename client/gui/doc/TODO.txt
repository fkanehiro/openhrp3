■検討項目(作業内容が具体的に確定したら作業項目へ移動。)
 ItemViewの右クリックメニューの在り方を検討する。
 manager.getItems(クラス)は、"クラス"しか返さない。"クラス"の親クラスまで含めるべきかどうかを検討する。
 バージョン管理の方法
 プロジェクトファイルに手入力以外で$(OPENHRPHOME)という表記を導入する方法を検討
 grxuirc.xmlをjarの中にいれデフォルト設定とし、変更があった場合はgui/grxuirc.xmlを生成する?
 
■作業項目 (終了後は終了項目へ移動。番号は単なるIDで優先順位ではない。)
9 ファイルロガー機能とメモリロガー機能(リングバッファ)の切替え機構追加
10 AVI形式で録画できるようにする。
11 ロボットをぐりぐりと押す機能追加
13 grxuirc.xmlのロードを、GrxXMLRepositoryから、GrxProjectItemに
16 二つめのGrxUIが上がる時にダイアログを出す。
17 3DView上にカーソルキーで操作ができる旨を示す簡易インストラクションを表示
21 ロボット姿勢のプロジェクトファイル上での表現方法 AngleAxis -> クォータニオンに変更
22 マルチコアPC上で、Simulation実行中にplaybackすると、時々最新の情報が表示される。
31 GraphのSeriesDialogで、ログをとってないデータを選べないようにする。
32 Graph表示で、ログをとっていないデータは飛ばすようにする。
36 GrxTextItemに、Documentを導入。
45 ViewSimulator。
47 3DView上にZMPを表示する
48 RobotStateView 上にZMPを表示する
49 Graph上にZMPを表示する
50 3DView上にZMPや重心位置の軌跡を表示する


■終了項目
--rev??? 川角
1 PlayBack速度設定ボタン追加
13 3DView上でカーソルキーを押し、関節角度を編集する機能。数値はGrxRobotStatView上に表示
  
--rev667 川角
2	一部のISEファイル(***Dialog等)をcom/generalrobotix/ui/util以下に移動。
3 images -> resources/images に統合。

--rev668 川角
4 	ファイルログ機能(GrxWorldStateItem)のバグ取り(EOFエラー問題)
5	Simulationのくり返しテストをPythonInterpreterから実行するため、GrxOpenHRPViewにnon-interactiveモードを追加
＊ 4のチェックに5を利用。5秒間×50回シミュレーションを繰り返したが問題は起きなかった。
7 JTable(GrxRobotStatView)
  time変動でTable全体をリペイント
   > 値が変わったら、cellのみを書き換える。
  Simulation中でなければ、多少負荷が増えるが、シミュレーション中の速度は早くなっているようだ。
8 FrameRate設定バーの表示・非表示機能追加

--rev669 
18 GrxRobotStateViewにインストラクションメッセージを追加
19 シミュレーションログのロード直後の時刻を、終了時刻から現在時刻にもってくるため、ロードファンクションをGUIスレッドから別スレッドに移した
20 GrxWorldStateItemのログ保存先をGUI_DIR/logに変更
 
--rev670
  GrxBaseItem.setProperty , getProperty , storeProperty , restorProperty回りを整理

--rev671
23 複数のWorldStateItemがある場合を想定して、log/itemName/以下にログファイルを置く事にした。
24  rev670のbug fix: GrxOpenHRPView Gravityの設定を修正
  
--rev683
25 OnlineViewerのCORBAサーバ機能を追加
26 grxuirc.xmlのwin/lin共有化
27 ./GrxUI.sh project mode script debug とする事でデフォルト値を設定。

--rev684
28 simulation 終了後にかかった実時間を表示
29 jointTorqueをログに追加
30 GrxSeqEditViewを追加

--rev685
33 Grx3DViewで、WireFrameモードにしてから通常モードに戻すと、
   リンクのバウンディングボックス(Switchノード)がWireFrameでなくなる問題を解決。
34 Grx3DViewのRecordingが終了しない問題を解決
35 GrxSeqEditViewの左右入れ換え機能のbugfix

--rev704
37 関節角度の限界値がVRMLファイルに入っていない場合は、リミットを無効にする。
38 ３DView上での上下キーによる選択関節の変更時の順序をJointID順に変更。
12 GrxModelItemのプロパティにmodelType(Robot,Environment)を追加。
39 Robotモデルの重心位置とZ=0平面への投影点の表示/非表示機能追加。(ただし、アイコンがないのでdisable中)
40 Controller設定ペーン上にRobotモデルのみを表示するように変更。
   ProcessManagerのコードを少し整理
6  グラフの設定を、GrxGraphViewのプロパティから、GrxGraphItemに移す。
42 Grashの設定で、選択可能項目を絞りこんでから表示する。
43 GrxJythonInterpreterで、実行するスクリプトのディレクトリがパスにない場合は追加してから実行する。
15 Python起動スクリプトの導入。->素人用モード等に有効。

--rev706
44 3DViewのZOOM方向の±を変更 (WalkViewHandler.java RoomViewHandler.java)

--rev714
41 プロセスの種類が増えてもGrxProcessManagerパネル上の右クリックMenuが更新されない問題を解決
14 WindowConfigをプロジェクトファイルにいれる。
46 3DView上に重心を表示する

--rev715
47 CollisionPair設定パネルのaddAllボタンを実装。
その他コードの整理を行った

--rev730
51 waitInput*を導入する

--rev7??
52 プロジェクトをロード時に、VRMLモデル内に設定されたWAISTリンクの位置が反映されない

--rev751
53 OpenHRPタブでシミュレーションの総時間を変更し，プロジェクトをSaveしても変更が反映されていない
54 Save Projectをすると必ずファイル名を確認される（Save asがないので当然）のがよくあるアプリの挙動と異なる
55 スクリプトで待ち状態に入ったままでシミュレーションが終了してしまうとPython Interpreterのタブが使えなくなってしまう．-> script interrupt機能導入
